#include<stdio.h>
struct node*{
    int data;
    struct node *next;
};
int main(){
    struct node *new;
    struct node *head;

    struct node *temp=head;
       new=(struct node*)malloc(sizeof(struct node));
       new.next=NULL;
        printf("enter data");
        scanf("%d",&new.data);
        while(temp.next!=NUll){
            temp=temp.next;
        }
        temp.next=new;
}